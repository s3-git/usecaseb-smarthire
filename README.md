# AI-powered Talent Acquisition Platform

### Environment Setup
1. Run `make setup` to setup neccessary services 

2. Run `make run` to start server, running at http://localhost:3001

3. Run test with `make test`
